<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page-AuctionBazaar</title>
    <link rel="stylesheet" href="css/login.css">
</head>
<body>
    <div class="container">
        <div class="left-section">
            <h1>Welcome back</h1>
            <p>Please enter your details</p>
            <form method="POST" action="/home">
                <label for="email" name="email">Email address</label>
                <input type="email" id="email" name="email" required >

                <label for="password" name="password">Password</label>
                <input type="password" id="password" name="password" required>
                <% if (error) { %>
                    <p style="color: red; border: 1px solid red; padding: 10px;">
                        <%= error %>
                    </p>
                <% } %>

                <div class="options">
                    <label>
                        <input type="checkbox" name="remember"> Remember for 30 days
                    </label>
                    <a href="#">Forgot password</a>
                </div>

                <form >
                    
                    <button type="submit" class="sign-in-btn">Sign in</button>

                </form>
                <button type="button" class="google-btn">Sign in with Google</button>
            </form>
        
           <p class="signup-text">Don't have an account? <a href="http://localhost:3000/login/new">Sign up</a></p>

        </div>
        <div class="right-section">
            <div class="illustration"></div>
        </div>
    </div>
</body>
</html>
